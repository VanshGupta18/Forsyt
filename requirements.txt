# GKG GPR Research Pipeline dependencies
pandas>=2.2
numpy>=1.26
pyarrow>=15.0        # Parquet read/write
requests>=2.32       # GDELT downloads + Wayback/CC fetches
scipy>=1.13          # Validation statistics
matplotlib>=3.9      # Plotting
openpyxl>=3.1        # Caldara Excel download in validate_gpr.py

# India newspaper path (scraper/ package)
beautifulsoup4>=4.12 # HTML parsing in scraper/outlets/*.py
lxml>=5.0            # Fast BS4 parser backend
feedparser>=6.0      # RSS feed parsing (scraper/outlets)
python-dateutil>=2.9 # Flexible date parsing in backfill_cdx.py
aiohttp>=3.9         # Async HTTP client for scraper/scheduler.py
httpx>=0.27          # Async HTTP client for scripts/backfill_cdx.py
flask>=3.0           # Read API (scraper/api.py)
flask-restful>=0.3.10
gunicorn>=22.0       # Production WSGI for scraper/api.py

# DistilBERT theme + tone tagging
transformers>=4.40   # pipeline("sentiment-analysis")
sentence-transformers>=3.0  # SentenceTransformer embeddings
torch>=2.2           # CPU build is sufficient (no CUDA required)